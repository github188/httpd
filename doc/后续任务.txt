1、如果有黑客给你建很多个链接，每个链接就发4个字节。你的服务器会不会因为hold住太多链接而挂掉
2、支持主流静态文件，比如图片、html、js、css(finish)
3、非主流静态文件下载，比如请求一个pdf，一个word什么的，你要允许人家下载(finish)
4、支持断点续传(finish)
5、读文件是从磁盘读数据进内核空间，然后拷贝到用户空间。发送数据是从用户空间拷贝数据到内核空间。所以发送静态文件相当于把文件在内核里无修改地拷了两次。能提升么？
6、单进程没法利用多个cpu。能不能有利用多cpu的能力？(finish)
7、能利用多cpu以后，发现瓶颈在cpu0，表现是cpu0的负载会比其他cpu高不少？为什么，如何解决？(finish)
8、LOG需要可以被ctrl控制写不写,写什么类型的log.
9、写一个页面处理exit事件(out)
10、提高稳定性(心跳,复活)
11 重定向,选择下载服务器

顺序:6、7、2、3、4、1、5

bug&Remain
1.框架中对共享内存的占用情况未处理.
2.按下F5不放开会导致进程死掉.
3.框架进程未处理EPOLLOUT饿死问题.